---
import MarkdownSpecPage from '../../components/MarkdownSpecPage.astro';
import { getEntry } from 'astro:content';
import { ui } from '../../i18n';

const t = ui.zh;
const entry = await getEntry('specs_zh' as any, 'now');
const updated = typeof entry?.data?.updated === 'string' ? entry.data.updated : undefined;
---

<MarkdownSpecPage lang="zh" mdId="now" eyebrow={t.nowEyebrow} title={t.nowTitle} updated={updated} />
