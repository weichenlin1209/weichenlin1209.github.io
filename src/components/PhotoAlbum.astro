---
// 相册详情组件：展示单个相册的全部照片，支持点击放大（Lightbox）
// 图片来自 src/assets/album/，构建时用 astro:assets 自动优化
import { getImage } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import AnimateIn from './AnimateIn.astro';
import { ui } from '../i18n';
import type { Lang } from '../site.config';

interface Props {
  lang: Lang;
  album: string;
}

const { lang, album } = Astro.props;
const t = ui[lang];

// 动态导入所有相册图片（构建时静态分析）
const albumModules = import.meta.glob(
  '/src/assets/album/**/*.{jpg,jpeg,png,webp,gif,avif}',
  { eager: true, import: 'default' },
) as Record<string, ImageMetadata>;

// 筛选当前相册的图片，生成缩略图（800px）+ 大图（1920px）
const entries = Object.entries(albumModules)
  .filter(([p]) => p.includes(`/${album}/`))
  .sort(([a], [b]) => a.localeCompare(b));

const photos: { thumb: string; full: string }[] = [];
for (const [, img] of entries) {
  try {
    const thumb = await getImage({ src: img, width: 800, format: 'webp' });
    const full = await getImage({ src: img, width: 1920, format: 'webp' });
    photos.push({ thumb: thumb.src, full: full.src });
  } catch {
    // 无法优化的图片跳过
  }
}
---

<BaseLayout title={`${album}`}>
  <div class="max-w-4xl mx-auto px-6 pt-24 pb-16">
    <a href={`/${lang}/photo`} class="text-sm text-fg/40 hover:text-accent transition-colors">{t.backToPhoto}</a>
    <h1 class="text-3xl font-bold mt-2 mb-1">{album}</h1>
    <p class="text-sm text-fg/40 mb-8">{t.photosCount(photos.length)}</p>

    {photos.length === 0 ? (
      <p class="text-fg/50">{t.noResults}</p>
    ) : (
      <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
        {photos.map((photo, i) => (
          <AnimateIn delay={i * 60}>
            <button
              class="lightbox-trigger w-full aspect-square rounded-xl overflow-hidden
                     ring-1 ring-fg/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent
                     group"
              data-index={i}
              data-full={photo.full}
              aria-label={t.viewPhoto(i + 1)}
            >
              <img
                src={photo.thumb}
                alt={`${album} ${i + 1}`}
                loading="lazy"
                class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
              />
            </button>
          </AnimateIn>
        ))}
      </div>
    )}
  </div>

  <!-- Lightbox 灯箱 -->
  <div
    id="lightbox"
    class="fixed inset-0 z-[100] hidden items-center justify-center bg-black/85 backdrop-blur-sm"
  >
    <button id="lightbox-close" class="absolute top-5 right-5 p-2 rounded-full bg-white/10 hover:bg-white/20 text-white transition-colors" aria-label="Close">
      <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
    <button id="lightbox-prev" class="absolute left-4 top-1/2 -translate-y-1/2 p-2 rounded-full bg-white/10 hover:bg-white/20 text-white transition-colors" aria-label="Previous">
      <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path d="M15 19l-7-7 7-7" />
      </svg>
    </button>
    <img
      id="lightbox-img"
      src=""
      alt=""
      class="max-h-[85vh] max-w-[85vw] rounded-xl shadow-2xl"
    />
    <button id="lightbox-next" class="absolute right-4 top-1/2 -translate-y-1/2 p-2 rounded-full bg-white/10 hover:bg-white/20 text-white transition-colors" aria-label="Next">
      <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path d="M9 5l7 7 7-7" />
      </svg>
    </button>
    <span id="lightbox-count" class="absolute bottom-5 left-1/2 -translate-x-1/2 text-white/70 text-sm font-mono"></span>
  </div>

  <script>
    const triggers = Array.from(document.querySelectorAll<HTMLButtonElement>('.lightbox-trigger'));
    const lightbox = document.getElementById('lightbox') as HTMLElement;
    const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
    const lightboxCount = document.getElementById('lightbox-count') as HTMLElement;

    // 灯箱显示全尺寸大图：从按钮的 data-full 读取（astro:assets 优化的 1920px）
    const srcs = triggers.map((btn) => btn.getAttribute('data-full') ?? '');

    let current = 0;

    function openLightbox(index: number) {
      current = index;
      lightboxImg.src = srcs[current] ?? '';
      lightboxImg.alt = `photo ${current + 1}`;
      lightboxCount.textContent = `${current + 1} / ${srcs.length}`;
      lightbox.classList.remove('hidden');
      lightbox.classList.add('flex');
      document.body.style.overflow = 'hidden';
    }

    function closeLightbox() {
      lightbox.classList.add('hidden');
      lightbox.classList.remove('flex');
      document.body.style.overflow = '';
    }

    function show(index: number) {
      current = (index + srcs.length) % srcs.length;
      lightboxImg.src = srcs[current] ?? '';
      lightboxImg.alt = `photo ${current + 1}`;
      lightboxCount.textContent = `${current + 1} / ${srcs.length}`;
    }

    triggers.forEach((btn) => {
      btn.addEventListener('click', () => openLightbox(Number(btn.dataset.index)));
    });
    document.getElementById('lightbox-close')?.addEventListener('click', closeLightbox);
    document.getElementById('lightbox-prev')?.addEventListener('click', () => show(current - 1));
    document.getElementById('lightbox-next')?.addEventListener('click', () => show(current + 1));
    lightbox.addEventListener('click', (e) => {
      if (e.target === lightbox) closeLightbox();
    });
    document.addEventListener('keydown', (e) => {
      if (lightbox.classList.contains('hidden')) return;
      if (e.key === 'Escape') closeLightbox();
      if (e.key === 'ArrowLeft') show(current - 1);
      if (e.key === 'ArrowRight') show(current + 1);
    });
  </script>
</BaseLayout>
